import 'package:flutter/material.dart';

class SBOVerticalDivider extends StatelessWidget {
  const SBOVerticalDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const VerticalDivider(
      color: Colors.black,
      thickness: 1,
      width: 1,
    );
  }
}
